{% extends 'base.html.twig' %}

{% block title %}Modifier User{% endblock %}

{% block body %}
<div class="V8">
    </div>
<h1 class="facturation">Facturation</h1>
<div class="facture">
    {{ form_start(form) }}
        <div class="forme">
        {{ form_row(form.date) }}
        </div>
         <div class="forme1">
        {{ form_row(form.client) }}
        </div>
        <div class="forme2">
        {{ form_row(form.colis) }}
        </div>
        <div class="forme3">
        {{ form_row(form.prixht) }}
        </div>
        <div class="forme4">
        {{ form_row(form.prixttc) }}
         </div>
    </div>
    
        <button class="valider" type="submit">Valider</button>
   
    {{ form_end(form) }}

{% endblock %}